# Client-side (VITE_ prefix = bundled into browser build — safe for Firebase public config only)
VITE_FIREBASE_CONFIG=

# Server-side only (no VITE_ prefix — Edge Functions only, never in browser bundle)
FIREBASE_PROJECT_ID=
GEMINI_API_KEY=
FIREBASE_SERVICE_ACCOUNT=
